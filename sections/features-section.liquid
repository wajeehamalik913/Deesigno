<div class="features-section section-padding">
  <div class="page-width">
    <div class="features-section__header">
      <h2 class="features-section__heading h1">{{ section.settings.heading }}</h2>
      <p class="features-section__text">{{ section.settings.text }}</p>
    </div>
    
    <div class="features-grid">
      {%- for block in section.blocks -%}
        {%- case block.type -%}
          {%- when 'feature' -%}
            <div class="feature-item">
              <div class="feature-item__icon">
                {{ block.settings.icon }}
              </div>
              <h3 class="feature-item__title h3">{{ block.settings.title }}</h3>
              <p class="feature-item__text">{{ block.settings.text }}</p>
            </div>
        {%- endcase -%}
      {%- endfor -%}
    </div>
  </div>
</div>

{% schema %}
{
  "name": "Features Section",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "default": "Why Choose Deesigno?",
      "label": "Heading"
    },
    {
      "type": "text",
      "id": "text",
      "default": "We make custom merchandise simple, affordable, and high-quality for businesses and individuals alike.",
      "label": "Description"
    }
  ],
  "blocks": [
    {
      "type": "feature",
      "name": "Feature",
      "settings": [
        {
          "type": "text",
          "id": "icon",
          "default": "🎨",
          "label": "Icon (emoji or text)"
        },
        {
          "type": "text",
          "id": "title",
          "default": "Custom Design",
          "label": "Title"
        },
        {
          "type": "textarea",
          "id": "text",
          "default": "Upload your design or work with our team to create something unique for your brand.",
          "label": "Description"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Features Section",
      "blocks": [
        {
          "type": "feature",
          "settings": {
            "icon": "🎨",
            "title": "Custom Design",
            "text": "Upload your design or work with our team to create something unique for your brand."
          }
        },
        {
          "type": "feature",
          "settings": {
            "icon": "⚡",
            "title": "Fast Turnaround",
            "text": "Quick production times without compromising on quality. Most orders ship within 3-5 business days."
          }
        },
        {
          "type": "feature",
          "settings": {
            "icon": "✨",
            "title": "Premium Quality",
            "text": "High-quality materials and printing techniques ensure your custom merchandise looks professional."
          }
        }
      ]
    }
  ]
}
{% endschema %}
